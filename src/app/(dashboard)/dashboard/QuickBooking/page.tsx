/*import { QuickBookingWidget } from "@/components/ui/quick-booking-widget";
import session from "@/hooks/session";

export default async function QuickBookingPage() {
  const sessionData = await session();
  
  return (
    <div>
      <QuickBookingWidget variant="full" patientId={sessionData.user?.id} />
    </div>
  );
}
*/

// app/voice-agent/page.tsx
// app/voice-agent/page.tsx
// app/voice-agent/page.tsx
'use client';

import React, { useState } from 'react';

export default function VoiceAgentPage() {
  
  return (
      <h1 className="text-2xl font-bold mb-4">Voice Agent</h1>
      
  );
}
